# Shared secret — PHẢI khớp AI_SERVICE_API_KEY trong .env của Backend
API_KEY=change-me
# Đường dẫn model weights (tải bằng: python scripts/download_models.py)
MODEL_M109_PATH=./models/manga109_yolo.pt
MODEL_BUBBLE_PATH=./models/bubble_seg.pt
# Ngưỡng confidence lọc box (optional). Rỗng = mặc định của model. Vd: 0.25
CONF_THRESHOLD=
