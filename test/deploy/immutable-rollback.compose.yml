services:
  app:
    image: ${DRILL_IMAGE:?DRILL_IMAGE must be an immutable image ID}
    restart: 'no'
    healthcheck:
      test: ['CMD-SHELL', '[ "$(cat /healthy)" = "true" ]']
      interval: 1s
      timeout: 1s
      retries: 3
      start_period: 1s
